{
  "license":"MIT",
  "scripts": {
    "migrate": "migrate -path ./migrations -database \"postgres://go_user:go_password@localhost:5433/api_go?sslmode=disable\" up",
    "generate": "sqlc generate",
    "dev": "air"
  }
}